import 'package:alfred/alfred.dart';

// Rename this file settings.dart and fill out the mongodb_url - delete this

// A map of our config settings
Map<String, dynamic> settings = {
  'mongodb_url': "",
  'mongodb_db_collection': 'messages',
  'port': 8080,
  'logLevel': LogType.info,
  'http_ip': 'localhost', // '0.0.0.0' or 'localhost' or a specific IP from your network
   'gh_repo': ""
};
